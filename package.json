{
  "name": "learning-playwright",
  "version": "0.1.0",
  "description": "Playwright examples and tests",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.35.0"
  },
  "engines": {
    "node": ">=16"
  }
}
